{
  "name": "company-website",
  "private": true,
  "version": "1.0.0",
  "description": "Cabinet Creation Co. website — static site with unit-tested vanilla JS modules",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
